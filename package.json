{
  "name": "shri-andinath-jinalay",
  "version": "0.1.0",
  "private": true,
  "description": "Temple website quality checks and local development tooling.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "format:check": "prettier --check package.json CONTRIBUTING.md eslint.config.mjs playwright.config.js vitest.config.js firebase.json .firebaserc firestore.indexes.json "functions/**/*.js" "functions/package.json" "migration/**/*.mjs" "migration/package.json" "scripts/**/*.mjs" "tests/**/*.js"",
    "lint": "eslint functions scripts tests",
    "test:unit": "vitest run",
    "test:functions": "node --test functions/test/*.test.js",
    "test:links": "node scripts/check-links.mjs",
    "test:ui": "cross-env PLAYWRIGHT_BROWSERS_PATH=0 playwright test",
    "build": "node scripts/build-pages.mjs",
    "check": "npm run format:check && npm run lint && npm run test:unit && npm run test:functions && npm run test:integration && npm run test:links && npm run test:ui",
    "test:integration": "vitest run tests/integration",
    "test:kshamawani-load": "node scripts/kshamawani-load-test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@eslint/js": "^9.35.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.35.0",
    "prettier": "^3.6.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
